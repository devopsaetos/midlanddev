This file is NOT loaded by the module manifest - it exists purely to preserve the following
notes/dead rows that used to live as invalid "#"-comment lines inside ir.model.access.csv.
Odoo's CSV loader does not support comments; any "#" line there is parsed as a data row and
breaks with "IndexError: list index out of range" on install. Do not restore these lines into
the .csv file verbatim - if ever needed, add them as proper 8-column rows.

The two lines below were leftover boilerplate from a different module template (they
reference models - maintenance_recovery_wizard / report_maintenance_recovery_report_
maintenance_recovery - that do not exist anywhere in this module; the wizard's actual
model is `maintenance.recovery.report`, not `maintenance.recovery.wizard`). Kept for
reference only; the correct access rule was added to ir.model.access.csv, following the
same fix precedent used in maintenance_collection_report/security/ir.model.access.csv.
The report.maintenance_recovery.maintenance_recovery_report AbstractModel used for the
QWeb report does not need an access rule, consistent with every other report
AbstractModel in this codebase - the report engine renders it via sudo.

access_maintenance_recovery_wizard,maintenance_recovery_wizard,model_maintenance_recovery_wizard,,1,1,1,1
access_report_maintenance_recovery_report_maintenance_recovery,report_maintenance_recovery_report_maintenance_recovery,model_report_maintenance_recovery_report_maintenance_recovery,,1,1,1,1
