<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record forcecreate="True" id="ir_cron_maintenance_charges" model="ir.cron">
            <field name="name">Maintenance Charges Invoice</field>
            <field name="model_id" ref="model_maintenance_charges"/>
            <field name="state">code</field>
            <field name="code">model.maintenance_charges_invoices()</field>
            <field name="interval_number">1</field>
            <field name="priority">0</field>
            <field name="interval_type">months</field>
            <!-- 'numbercall' and 'doall' fields were removed from ir.cron in later Odoo versions
                 (see real_estate/data/automate_action.xml for the same pattern) -->
            <!--            <field name="numbercall">-1</field>-->
            <!--            <field eval="True" name="doall"/>-->
        </record>
        <record id="ir_cron_society_charges" model="ir.cron">
            <field name="name">Electricity Charges Invoice</field>
            <field name="model_id" ref="model_maintenance_charges"/>
            <field name="state">code</field>
            <field name="code">model.society_charges_invoices()</field>
            <field name="interval_number">1</field>
            <field name="priority">0</field>
            <field name="interval_type">months</field>
            <!-- 'numbercall' and 'doall' fields were removed from ir.cron in later Odoo versions
                 (see real_estate/data/automate_action.xml for the same pattern) -->
            <!--            <field name="numbercall">-1</field>-->
            <!--            <field eval="True" name="doall"/>-->
        </record>
    </data>
</odoo>